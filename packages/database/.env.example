# Prisma reads DATABASE_URL. On Railway this is provided by the Postgres plugin
# as ${{ Postgres.DATABASE_URL }}. Locally, point at a dev database:
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/trustee?schema=public"
